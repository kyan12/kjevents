/** @type {import('next').NextConfig} */
const nextConfig = {
  // Domain redirects (kirajiaweddings.com -> kirajiaevents.com/weddings) 
  // are best handled via Vercel Project Domains configuration.
  // We remove the internal path redirect to prevent an infinite loop.
};

export default nextConfig;
